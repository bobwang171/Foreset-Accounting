import { defineComponent } from "vue"

export const Second = defineComponent({
    setup() {
        return () => (
            <div>
                Second
            </div>
        )
        
    }
})