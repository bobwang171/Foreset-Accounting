import { defineComponent } from "vue"

export const Third = defineComponent({
    setup() {
        return () => (
            <div>
                Third
            </div>
        )
        
    }
})